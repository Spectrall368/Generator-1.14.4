new ReplaceBlockConfig(Lists.newArrayList(<#list input_list$target as target>${target?keep_after("State(")?keep_before_last(")")}<#sep>,</#list>), Lists.newArrayList(<#list input_list$target as target>${target?keep_after("State(")?keep_before_last(")")}<#sep>,</#list>))
